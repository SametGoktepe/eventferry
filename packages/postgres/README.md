# @eventferry/postgres

PostgreSQL store for [eventferry](https://github.com/your-org/eventferry). Provides transaction-joining `enqueue`, a lock-free `claimBatch` (`FOR UPDATE SKIP LOCKED`), and migration SQL.

See the [root README](../../README.md) for usage.
