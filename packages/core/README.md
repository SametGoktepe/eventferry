# @eventferry/core

DB- and broker-agnostic core of **eventferry**. Contains the `Relay` loop, backoff strategies, serializer, and the `OutboxStore` / `Publisher` interfaces that the storage and broker packages implement.

See the [root README](../../README.md) for usage.
